# Feedback Loop Examples v1

If CST returns center frequency above target:

- Reduce effective electrical shortening by increasing patch length in next iteration.

If CST returns bandwidth below target:

- Increase substrate height or adjust feed width within constraints.

If VSWR is above threshold:

- Adjust feed offset and feed width before re-run.
